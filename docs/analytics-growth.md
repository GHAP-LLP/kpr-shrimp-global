# Analytics & Growth

Living document — current state, not a log. See `CHANGELOG.md` for history.

## Status: not instrumented

No analytics (Google Analytics, Plausible, etc.), no Search Console, no conversion tracking on forms (forms don't hit a backend yet — see `backend.md`). Nothing to report here yet.

## When this becomes relevant
- Once the backend is live and forms submit real data, add form-submission tracking.
- Once the custom domain is connected, set up Search Console for the live domain.
