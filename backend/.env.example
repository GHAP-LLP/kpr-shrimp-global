MONGO_URL=mongodb://localhost:27017
DB_NAME=kpr_shrimp
CORS_ORIGINS=http://localhost:3000,https://frontend-mu-nine-84.vercel.app

# Admin auth
JWT_SECRET=change-me-to-a-long-random-string
ADMIN_USERNAME=admin
# Generate with: python -c "import bcrypt; print(bcrypt.hashpw(b'yourpassword', bcrypt.gensalt()).decode())"
ADMIN_PASSWORD_HASH=$2b$12$replace-with-a-real-bcrypt-hash

# Email notifications (optional — skipped if unset). Sign up at https://resend.com
RESEND_API_KEY=
RESEND_FROM_EMAIL=notifications@yourdomain.com
ADMIN_NOTIFICATION_EMAIL=sales@indoaquaticltd.com
